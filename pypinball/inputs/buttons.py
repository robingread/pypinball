import enum


class Buttons(enum.Enum):
    """
    Eums for defining all the Physical Hardware input buttons for the game.
    """

    CENTER = enum.auto()
    LEFT = enum.auto()
    RIGHT = enum.auto()
